"""Built-in experiment configs for WD/RNN training.

YAML configs (.yaml) 放在此目錄下，自動被 registry 發現。
不需手動 import 或註冊。

使用方式:
  --experiment_config wd_sa2_a2c_aux          # 用名字
  --experiment_config /path/to/custom.yaml    # 用路徑

新增 config 只需在此目錄建立 .yaml 檔案即可。
"""
